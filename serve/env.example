# Copy to .env and `set -a; . ./.env; set +a` before `bash serve/serve.sh`.
API_KEY=change-me-long-random-string
MODEL_PATH=/root/model-glm53
SERVED_MODEL_NAME=glm-5.3-flash
# HF_TOKEN=hf_...   # only needed for `hf download` of gated/private repos; the checkpoint used here is public
